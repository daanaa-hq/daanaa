// Re-export from context so all components share the same state
export type { GivingListItem } from '../contexts/GivingListContext'
export { useGivingListContext as useGivingList } from '../contexts/GivingListContext'
