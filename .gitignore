# MemPalace per-project files (issue #185)
mempalace.yaml
entities.json

# Python
venv/
__pycache__/
*.pyc
*.pyo

# Node / frontend build
node_modules/
frontend/node_modules/
frontend/dist/
*.tsbuildinfo

# Data (large; sourced from IRS/ProPublica, not version-controlled)
data/
!frontend/src/data/

# Logs & runtime state
*.log
nohup.out
.state
.master_state.json
logs/
reports/

# Local agent / tooling state
.claude/
autodev/logs/

# Backups & archives
backups/
backup_*/
archive_*/
*.backup.*
*.broken.*

# Legacy / debris (see CLAUDE.md — not part of active codebase)
app.py
app.py.
app.py.api-only.*
app_with_feedback.py
merit_app.py
merit_daemon.py
merit_master.py
rebuild.py
ui_agent.py
test_load.py
inspect_csv.py
debug_app.py
fix*.py
patch.py
propublica_fix.py
cloudflared.deb
HEALTH_CHECK_REPORT.txt
VALIDATION_REPORT.txt
MERIT_ERROR_LOG.md
MERIT-STATUS-REVIEW.md
Agent C/
Agent E/
0]

# Separate / legacy sub-projects (not the canonical stack)
nonprofit-explorer/
static_web/
templates/
static/
web/
.gstack/
.env
audit_report.csv
archive/
frontend/src/data/*.db

# runtime/deploy artifacts (never commit)
.deploy_scratch/
.backups/
.gpu_queue.json
scores_v4_0_*.json
scores_v5_0_*.json

# Oversized generated artifacts — purged from git history 2026-06-10
# (GitHub 100MB hard limit; rebuilt by build_faiss_index.py / precompute scripts)
precompute_output/faiss_index.bin
precompute_output/faiss_index_dm.bin
precompute_output/search.db
precompute_archive/

# Precomputed per-org and browse static files (1.97M + 136K files; rebuilt by precompute_orgs.py / precompute_browse.py)
precompute_output/orgs/
precompute_output/browse/

# Firebase service account (private key — never commit)
firebase-service-account.json
firebase-service-account.json.odt
deploy/plausible/plausible-conf.env
