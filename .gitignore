# MemPalace per-project files (issue #185)
mempalace.yaml
entities.json

# Python
venv/
__pycache__/
*.pyc
*.pyo

# Node / frontend build
node_modules/
frontend/node_modules/
frontend/dist/
*.tsbuildinfo

# Data (large; sourced from IRS/ProPublica, not version-controlled)
data/
!frontend/src/data/

# Logs & runtime state
*.log
nohup.out
.state
.master_state.json
logs/
reports/

# Local agent / tooling state
.claude/
# ...but the custom skills corpus IS tracked (founder-directed 2026-07-17)
!.claude/
.claude/*
!.claude/skills/
autodev/logs/

# Backups & archives
backups/
backup_*/
archive_*/
*.backup.*
*.broken.*

# Legacy / debris (see CLAUDE.md — not part of active codebase)
app.py
app.py.
app.py.api-only.*
app_with_feedback.py
merit_app.py
merit_daemon.py
merit_master.py
rebuild.py
ui_agent.py
test_load.py
inspect_csv.py
debug_app.py
fix*.py
patch.py
propublica_fix.py
cloudflared.deb
HEALTH_CHECK_REPORT.txt
VALIDATION_REPORT.txt
MERIT_ERROR_LOG.md
MERIT-STATUS-REVIEW.md
Agent C/
Agent E/
0]

# Separate / legacy sub-projects (not the canonical stack)
nonprofit-explorer/
static_web/
templates/
static/
web/
.gstack/
# Secrets: ignore every .env variant. Only .env*.example templates may be committed
# (and only if they contain NO real values). See privacy_check.sh GATE 6.
.env
.env.*
.env.pre-*
!.env.example
!.env.*.example
audit_report.csv
archive/
frontend/src/data/*.db
graphify-out/
frontend/graphify-out/
scripts/graphify-out/

# runtime/deploy artifacts (never commit)
.deploy_scratch/
.backups/
.gpu_queue.json
scores_v4_0_*.json
scores_v5_0_*.json

# Oversized generated artifacts — purged from git history 2026-06-10
# (GitHub 100MB hard limit; rebuilt by build_faiss_index.py / precompute scripts)
precompute_output/faiss_index.bin
precompute_output/faiss_index_dm.bin
precompute_output/search.db
precompute_archive/

# Precomputed per-org and browse static files (1.97M + 136K files; rebuilt by precompute_orgs.py / precompute_browse.py)
precompute_output/orgs/
precompute_output/browse/

# Regenerable precompute outputs, same class as faiss_index.bin/search.db
# above — missing from this list until 2026-08-18 (task #17), which is why
# commit 0345fd9b7c3 (an unrelated org-page feature) showed a confusing
# diff deleting these. Confirmed both are write-only outputs, not required
# inputs: cohort_context.json is precompute_cohort_context.py's own
# documented Output; ein_map.json.gz is written by build_faiss_index.py
# (runs weekly). content/*.json.gz is written by the content-precompute
# step of the same pipeline family.
precompute_output/cohort_context.json
precompute_output/ein_map.json.gz
precompute_output/content/

# Firebase service account (private key — never commit)
firebase-service-account.json
firebase-service-account.json.odt
deploy/plausible/plausible-conf.env
scores_v5_0_20260615.json
.aws-backup-config

# Visibility/overlay generated artifacts (regenerate via scripts/generate_visibility_exports.py)
# — but the pipeline scripts are hand-written source, not artifacts: track them.
# (visibility/config stays ignored: it holds the IndexNow key.)
visibility/*
!visibility/scripts/
visibility/scripts/__pycache__/
dist/
.wrangler/

# Patch manifests (scratch output from patch_precompute_v5_context.py)
precompute_output/v5_patch_changed.txt

# Session/agent state and generated marketing artifacts — never track
.superpowers/
scripts/linkedin/output/
precompute_output/offline_pack/
.gpu_override_until
